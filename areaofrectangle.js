// Function to calculate the area of a rectangle
function rectangleArea(length, width) {
    return length * width;
}

// Example usage
let length = 10;
let width = 5;
console.log(`The area of the rectangle is: ${rectangleArea(length, width)}`);
